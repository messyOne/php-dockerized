nginx:
  build: .
  ports:
    - "80:80"
    - "443:443"
    - "9000:9000"
  links:
    - postgresql:postgresql
    - mongo:mongo
    - memcached:memcached
    - redis:redis
    - elasticsearch:elasticsearch
  volumes:
    - /mnt/www/php-dockerized/www:/var/www
    - /mnt/www/php-dockerized/sites:/etc/nginx/conf.d
    - /mnt/www/php-dockerized/logs:/var/log/supervisor

postgresql:
  image: postgres
  ports:
    - "5432:5432"

mongo:
  image: mongo
  ports:
    - "27017:27017"

memcached:
  image: memcached
  ports:
    - "11211:11211"

redis:
  image: redis
  ports:
    - "6379:6379"

elasticsearch:
  image: elasticsearch
  ports:
    - "9200:9200"
    - "9300:9300"
